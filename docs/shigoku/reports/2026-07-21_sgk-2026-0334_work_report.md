---
task_id: SGK-2026-0334
doc_type: work_report
status: done
parent_task_id: SGK-2026-0320
related_docs:
  - docs/shigoku/subtasks/done/2026-07-01_sgk-2026-0334_p1b-shigoku-ops-decision-tree-cli_subtask_plan.md
  - docs/shigoku/subtasks/done/2026-06-29_sgk-2026-0322_reconstate-completion-parallel-checkpoint-decision-tree_subtask_plan.md
  - docs/shigoku/plans/done/2026-06-29_sgk-2026-0320_recon-resume-visibility-conversational-ops_plan.md
  - docs/shigoku/subtasks/done/2026-06-24_sgk-2026-0300_run-narrative-target-profile-markdown_subtask_plan.md
  - docs/shigoku/worklogs/2026-07-21_sgk-2026-0334_work_log.md
created_at: '2026-07-21'
updated_at: '2026-07-28'
title: 'SGK-2026-0334 作業完了報告書: 判断ツリー可視化＋shigoku-ops decision-tree CLI'
---

## 1. 達成したゴール

- [x] `shigoku-ops report decision-tree --session <path>` または `--report <path>` を実行すると、Recon→MC→Swarm入口→Report の判断と結果が、一次証拠由来の Markdown / Mermaid ツリーとして読める。
- [x] 運用者が失敗ノード、重要判断、再開判断だけを絞り込んで見られ、中断後の再実行判断に使える。
- [x] 巨大 session や親子リンク欠落があっても壊れず、縮約表示や degrade 表示で「読めるが推測しない」出力になる。

## 2. 実装内容

### 2.1 新規追加

| ファイル | 内容 |
|---|---|
| `src/reporting/decision_tree_formatter.py` | `DecisionTreeFormatter` — 内部 contract（`DecisionTreeNode`, `DecisionTreeEdge`, `DecisionTreeSummary`）とツリー構築ロジック。`run_ledger` を主系（正本）、`decision_traces` を legacy fallback、`parent_event_id` を第一優先、`decision_id`/`task_id` を補助キーとした系列復元。Mermaid `graph TD` + Markdown summary + degrade summary レンダリング。上限制限（max_nodes=80, max_edges=120, max_depth=10, max_children_per_node=8）超過時の degradation。nested dict/list を含む secret/PII redaction。Mermaid/Markdown 文字列 escape。 |
| `tests/unit/reporting/test_decision_tree_formatter.py` | 39 tests: 基本構築、レガシー session、親子リンク、unlinked/estimated、巨大 session 縮約、フィルタリング(phase/actor/only-failures)、stable sort、checkpoint metadata、decision_id/task_id 補助リンク、degraded_nodes 集計、degrade_reasons dedup、redaction(JWT/Bearer/api_key)、escape、空データ耐性。 |

### 2.2 変更

| ファイル | 内容 |
|---|---|
| `scripts/shigoku_ops_cli.py` | `report decision-tree` サブコマンド追加。`--session`/`--report`/`--sessions-dir`/`--output`/`--phase`/`--actor`/`--only-failures`/`--max-nodes`/`--max-edges`/`--max-depth`/`--max-children-per-node`/`--json`/`--json-envelope`。`--report` 時は consistency check（`consistent` のみ生成）。`VALIDATION_SUITES["report"]` 更新。 |
| `tests/unit/scripts/test_shigoku_ops_cli.py` | +11 tests: JSON basic, output file, only-failures filter, phase filter, actor filter, max-nodes limit, json-envelope, blocked (no session), legacy session, inconsistent report block, `--json --output` writes file. |

## 3. 検証結果

- targeted tests: **83 passed**, 0 failures
- reporting regression: **618 passed**, 0 regressions
- docs validation: **0 errors** (FRONT_MATTER=0, BROKEN_LINKS=0, REGISTRY=0, DEFERRED_LINK=0)
- real session CLI validation: Exit code 0, correct Mermaid/Markdown output

## 4. 残課題 (deferred_tasks)

deferred_tasks:
  - deferred_id: SGK-2026-0334-D01
    title: 'Swarm内部判断ログの decision tree 統合'
    reason: '本タスクは Recon→MC→Swarm入口→Report に限定し、Swarm内部詳細（thought/action ループ）は対象外。詳細統合は SGK-2026-0293 系の設計に委ねる'
    impact: medium
    tracking_task_id: SGK-2026-0293
    recommended_next_action: 'SGK-2026-0293 系で execution trace の粒度と decision tree 連携契約を設計する'

  - deferred_id: SGK-2026-0334-D02
    title: '実レポート経路での end-to-end 検証'
    reason: 'targeted unit tests と real session CLI 検証は実施したが、実際の haddix_report 経由の `--report` consistency check 成功経路は未検証'
    impact: medium
    tracking_task_id: SGK-2026-0320
    recommended_next_action: '実際の haddix_report_*.md と session が consistent な組み合わせで `--report` 経路の decision-tree 生成を確認する'
