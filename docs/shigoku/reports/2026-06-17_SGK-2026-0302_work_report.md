---
task_id: SGK-2026-0302
doc_type: work_report
status: done
parent_task_id: SGK-2026-0065
title: '作業完了報告: RAG Module 分割 (SGK-2026-0302)'
created_at: '2026-06-17'
updated_at: '2026-06-18'
related_docs:
- docs/shigoku/plans/2026-06-17_rag-module-split_plan.md
- tests/unit/core/rag_module/test_rag_split.py
- tests/unit/commands/test_rag_commands_split.py
tags:
- shigoku
- rag
- refactoring
---

# 作業完了報告: RAG Module 分割 (SGK-2026-0302)

## 実施結果サマリ

`src/core/rag_module/rag.py` (1,078行) を facade (21行) + 4つの sibling module に分割した。全体的な完了状態: **facade分割は完了、ingest import path 修正済**。

## 行数削減実績

| ファイル | Before (行) | After (行) | 削減 | 計画目標 | 判定 |
|---|---|---|---|---|---|
| `rag.py` (facade) | 1,078 | 21 | -1,057 | <200 | ✓ |
| `rag_types.py` (新規) | — | 25 | — | <120 | ✓ |
| `rag_pdf_ingester.py` (新規) | — | 116 | — | 150-300 | ✓ (下限付近) |
| `rag_ingester.py` (新規) | — | 732 | — | 350-550 | ✗ (目標超過、残リスク) |
| `rag_switch.py` (新規) | — | 225 | — | 150-300 | ✓ |
| **合計** | **1,078** | **1,119** | **+41** | — | モジュールヘッダ増による微増 |

- `rag.py` 本体: Before 1,078行 → After 21行 (98.1%削減)
- AST行数の内訳は class/function 単位で分割したため対象メソッドすべてが分割先へ移動済み
- 合計行数の微増(+41行)は各モジュールの docstring と import header によるもの

## 実装詳細

### 作成/修正ファイル一覧

**新規ファイル (4):**
- `src/core/rag_module/rag_types.py` — `RAGDocument`, `RAGResult` dataclass
- `src/core/rag_module/rag_pdf_ingester.py` — `PDFIngester` クラス
- `src/core/rag_module/rag_ingester.py` — `KnowledgeIngester` クラス
- `src/core/rag_module/rag_switch.py` — `RAGSwitch`, `get_rag_switch()`, `init_rag()`

**修正ファイル (3):**
- `src/core/rag_module/rag.py` — facade化: 全実装を re-export に置換 (1,078→21行)
- `src/commands/rag.py` — `run_rag_ingest()` の破損 import を修正 (`src.core.rag` → `src.core.rag_module.rag`)
- `docs/shigoku/plans/2026-06-17_rag-module-split_plan.md` — チェックボックス完了反映

**新規テストファイル (2):**
- `tests/unit/core/rag_module/test_rag_split.py` — characterization test (24 tests: import, singleton, dataclass, RAGSwitch, init_rag idempotence)
- `tests/unit/commands/test_rag_commands_split.py` — command-layer regression test (5 tests: ingest/query/stats import paths, same-surface check)

**未変更ファイル:**
- `src/core/rag_module/__init__.py` — 空のまま維持 (facade再exportでpackage export不要)
- `src/commands/hunt.py` — 変更不要 (`get_rag_switch()` via facade)
- `src/cli/commands.py` — 変更不要 (`RAGSwitch` via facade)

### アーキテクチャ方針遵守

- 平置き sibling module 構成（`rag/` サブパッケージ化なし）
- facade は純粋な import re-export のみ（import時副作用なし）
- `service は MasterConductor instance を保持しない` 原則: 本タスクは MC 分割ではなく RAG モジュール分割のため適用対象外
- `task_queue`, `completed_tasks`, `execution_log`, `pending_hitl`, `_state_lock`, `context`, `event_bus` の final mutation: 適用対象外 (RAG module)
- 新しい god service の作成なし

## テスト結果

### 対象テスト（全）
```bash
.venv/bin/pytest tests/unit/core/rag_module/test_rag_split.py tests/unit/commands/test_rag_commands_split.py -v
```
- 実行件数: 29 tests (24 rag_split + 5 command-layer)
- 結果: **29 passed, 0 failed**
- pre-existing failure: 0
- 内訳:
  - `TestRagImportSurface`: 8 tests (import smoke, 複合一括import)
  - `TestRagSwitchSingleton`: 3 tests (singleton, init_rag callable)
  - `TestInstantiation`: 4 tests (KnowledgeIngester, PDFIngester, RAGSwitch)
  - `TestDataclassSmoke`: 2 tests (RAGDocument, RAGResult)
  - `TestRagSwitchMethods`: 4 tests (toggle, enable/disable, query, set_ingester)
  - `TestInitRagIdempotence`: 3 tests (disabled no-init, 2回呼び出し, singleton reuse) — **plan §3.1 line 68 に対応**
  - `TestRagCommandsImportPaths`: 5 tests (ingest/query/stats imports, bad-path safety, same-surface check) — **plan §3.1 line 69 に対応**

### コマンド固有テスト
```bash
.venv/bin/pytest tests/unit/commands -k rag -v
```
- 実行件数: 5 tests
- 結果: **5 passed, 0 failed**

### コンパイル検証
```bash
.venv/bin/python -m compileall -q src/core/rag_module/ src/commands/rag.py src/commands/hunt.py src/cli/commands.py
```
- 結果: 全ファイル 0 errors

### 全テストとの混同注意
本タスクの「対象テスト」は上記 29 tests。全テストスイートは未実行 (pre-existing failures の影響範囲外であるため)。

## 完了条件達成状況

| 完了条件 | 達成 | 備考 |
|---|---|---|
| rag.py が facade 化され 200 行未満 | ✓ | 21行 |
| import path 互換維持 | ✓ | 全7シンボル確認 + 破損 import 修正済み |
| characterization test 追加 | ✓ | 29 tests (import smoke + idempotence + command-layer) |
| command layer import 互換 | ✓ | ingest import path 修正、query/stats は元から正常 |

## 残リスク

以下の項目は first pass で未解決だが、計画当初から認識されていた制約であり、本タスクの完了を妨げない。

1. **`rag_ingester.py` が 732 行 (目標 350-550 超過)**
   - 原因: `KnowledgeIngester` が最も大きい責務を持つクラスであり、class 単位の切り出しでは目標範囲に収まらない
   - 残削減見込み: 二段分割（ingest系 / query系 / PDF委譲系）で 200-300行削減可能
   - 対応: 計画書 §5 で「今回は first pass として class 単位の切り出しまでに留める」と明記済み
   - 次タスク: 必要に応じてフォローアップタスクを起票

2. **`src/commands/rag.py` の `run_rag_ingest()` 破損 import を修正済み (line 25)**
   - 旧: `from src.core.rag import KnowledgeIngester` (broken, `src/core/rag.py` は不在)
   - 新: `from src.core.rag_module.rag import KnowledgeIngester` (facade re-export 経由)
   - `run_rag_query()` / `run_rag_stats()` は元から正しい path を使用しており変更不要

3. **`src/core/rag_module/__init__.py` が空**
   - 計画書 §5 で「package export 方針が未確定。まず facade 互換を優先」と明記
   - facade re-export により `from src.core.rag_module.rag import X` は正常動作

## 参照ルール

本タスクで参照したルールファイル:
- `rules/codingrules.md` — コード品質、エラーハンドリング、テスト規則
- `rules/task-ledger.md` — タスク台帳ワークフロー
- `rules/shigoku-docs.md` — SHIGOKUドキュメント規則
