---
task_id: SGK-2026-0302
doc_type: work_log
status: done
parent_task_id: SGK-2026-0065
title: '作業ログ: RAG Module 分割 (SGK-2026-0302)'
created_at: '2026-06-17'
updated_at: '2026-06-18'
related_docs:
- docs/shigoku/plans/2026-06-17_rag-module-split_plan.md
- docs/shigoku/reports/2026-06-17_SGK-2026-0302_work_report.md
- tests/unit/core/rag_module/test_rag_split.py
- tests/unit/commands/test_rag_commands_split.py
tags:
- shigoku
- rag
- refactoring
---

# 作業ログ: RAG Module 分割 (SGK-2026-0302)

## 時系列

### 2026-06-17

1. **Step 1: Import surface 調査**
   - 公開シンボル: `KnowledgeIngester`, `PDFIngester`, `RAGSwitch`, `RAGDocument`, `RAGResult`, `get_rag_switch()`, `init_rag()`
   - 消費者: `src/commands/rag.py` (2パス), `src/commands/hunt.py` (1パス), `src/cli/commands.py` (1パス)
   - 現行 `rag.py`: 1,078行

2. **Step 2: Characterization test 作成**
   - `tests/unit/core/rag_module/test_rag_split.py` 新設 (21 tests)
   - 全テストが分割前のコードベースで PASS 確認済み

3. **Step 3: rag_types.py 抽出**
   - `RAGDocument`, `RAGResult` を `rag_types.py` (25行) に抽出
   - `rag.py` から定義を削除し re-import に置換
   - テスト: 21/21 PASS

4. **Step 5→6→4: 依存順に残り3モジュール抽出**
   - `rag_pdf_ingester.py` (116行): `PDFIngester` 抽出
   - `rag_ingester.py` (732行): `KnowledgeIngester` 抽出
   - `rag_switch.py` (225行): `RAGSwitch` + singleton helpers 抽出
   - 依存順: rag_types → rag_pdf_ingester → rag_ingester → rag_switch (計画書の 3→4→5→6 の順序を依存関係に合わせて調整)

5. **rag.py facade 化**
   - 全実装を削除し、4 sibling module からの re-export のみに
   - 結果: 1,078行 → 21行

6. **Step 7-8: 検証**
   - `__init__.py`: 空のまま変更不要を確認
   - `compileall -q`: 全対象ファイル 0 errors
   - コマンドレイヤー消費者: import パス変更不要を確認
   - インポート・インスタンス化 smoke test: 全成功

7. **ドキュメント作成**
   - 計画書チェックボックス更新
   - work_report 作成
   - work_log 作成 (本ファイル)

## 判断記録

- 依存順の調整: 計画書の Step 3→4→5→6 を、実際の依存関係に合わせて 3→5→6→4 に変更した。rag_switch.py が rag_ingester.py に依存するため。
- __init__.py は空のまま: facade re-export で package export の必要性がないことを確認。計画書 §5 に「package export 方針が未確定」とあるため、無理な変更を避けた。
- KnowledgeIngester 732行: 計画書で想定された制約（first pass では class 単位の切り出しまで）の範囲内。二段分割は follow-up タスク。

## 参考にしたルールファイル

- `rules/codingrules.md`
- `rules/task-ledger.md`
- `rules/shigoku-docs.md`

## フォローアップ修正 (review findings)

### 2026-06-17 (review fix)

**Finding [High]**: `src/commands/rag.py` line 25 の `from src.core.rag import KnowledgeIngester` が破損 (`src/core/rag.py` は不在)。`run_rag_ingest()` 呼び出しで ImportError が発生。

**修正**: `src/core.rag` → `src/core.rag_module.rag` に修正。`run_rag_query()` / `run_rag_stats()` は元から正しい path のため変更不要。

**Finding [Medium]**: テストが command-layer 回帰と `init_rag()` の idempotence を捕捉していなかった。

**修正**:
- `tests/unit/commands/test_rag_commands_split.py` 新設: command-layer import path 回帰 (5 tests)
- `tests/unit/core/rag_module/test_rag_split.py` に `TestInitRagIdempotence` 追加 (3 tests, mock 使用)

**再検証**:
```bash
.venv/bin/pytest tests/unit/core/rag_module/test_rag_split.py tests/unit/commands/test_rag_commands_split.py -v
# 29 passed, 0 failed

.venv/bin/python -m compileall -q src/core/rag_module/ src/commands/rag.py src/commands/hunt.py src/cli/commands.py
# 全ファイル 0 errors
```

**レポート修正**: work_report の完了文言を「facade分割は完了、ingest import path 修正済」に更新。
